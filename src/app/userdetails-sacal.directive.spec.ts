import { UserdetailsSacalDirective } from './userdetails-sacal.directive';

describe('UserdetailsSacalDirective', () => {
  it('should create an instance', () => {
    const directive = new UserdetailsSacalDirective();
    expect(directive).toBeTruthy();
  });
});
